name: Validate docs and style

on:
  push:
    branches:
      - "docs/*"
      - "style/*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  code-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
      - run: pnpm lint:css
      - run: pnpm format:check
      - run: pnpm check:shai-hulud
