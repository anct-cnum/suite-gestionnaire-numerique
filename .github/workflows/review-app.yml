name: Clever Cloud Review App

on:
  workflow_dispatch:
    inputs:
      action:
        description: "action"
        required: true
        default: déploiement
        type: choice
        options:
          - deploy
          - update
          - delete
jobs:
  deploy:
    if: github.event.inputs.action == 'deploy'
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read
    environment:
      name: review/${{ github.event.number }}
    steps:
      - run: echo "Le nom de la branche est ${{ github.ref_name }} et le nom du repository est ${{ github.repository }}"
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.ref_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - run: echo "Le repository cloné est ${{ github.repository }} a été cloné dans le runner de GitHub"
      - run: echo "🚀 Le workflow est prêt à tester le code dans le runner"
      - run: |
          ls ${{ github.workspace }}
      - run: echo "Le statut du job est ${{ job.status }}"
      - name: Installation de clever-tools
        run: npm install -g clever-tools
      - name: Création et deploiement de l'application
        env:
          CLEVER_TOKEN: ${{ secrets.CLEVER_TOKEN }}
          CLEVER_SECRET: ${{ secrets.CLEVER_SECRET }}
          ORGA_ID: ${{ secrets.CLEVER_ORGA_ID }}
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          echo "Le nom de l'environnement est $BRANCH_NAME"
          clever create --type static-apache "$BRANCH_NAME" --alias "$BRANCH_NAME" --region par --org "$ORGA_ID"
          clever env set CC_WEBROOT "/out" --alias "$BRANCH_NAME"
          clever env set CC_PRE_BUILD_HOOK "npm install -g pnpm && pnpm install && pnpm build" --alias "$BRANCH_NAME"
          clever env set CC_OVERRIDE_BUILDCACHE "/out" --alias "$BRANCH_NAME"
          clever domain add "$BRANCH_NAME.cleverapps.io" --alias "$BRANCH_NAME"
          clever deploy --alias "$BRANCH_NAME"
  update:
    if: github.event.inputs.action == 'update'
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - name: install clever-tools
        run: npm install -g clever-tools
      - name: Link and update app
        env:
          CLEVER_SECRET: ${{ secrets.CLEVER_SECRET }}
          CLEVER_TOKEN: ${{ secrets.CLEVER_TOKEN }}
          ORGA_ID: ${{ secrets.CLEVER_ORGA_ID }}
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          clever link -o "$ORGA_ID" "$BRANCH_NAME" || echo "Application $BRANCH_NAME not found, skipping link."
          clever deploy --alias "$BRANCH_NAME" --force
  delete:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    if: github.event.inputs.action == 'delete'
    steps:
      - name: install clever-tools
        run: npm install -g clever-tools
      - name: Supprimer l'application
        env:
          CLEVER_SECRET: ${{ secrets.CLEVER_SECRET }}
          CLEVER_TOKEN: ${{ secrets.CLEVER_TOKEN }}
          ORGA_ID: ${{ secrets.CLEVER_ORGA_ID }}
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          clever link -o "$ORGA_ID" "$BRANCH_NAME"
          clever delete --alias "$BRANCH_NAME" --yes